using Cassandra;

namespace MediaPlayer.Helper;

public static class ValidationHelper
{
    public static bool ArgsValidation(string message)
    {
        Console.WriteLine(message);
        return false;
    }
}